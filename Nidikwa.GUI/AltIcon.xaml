<UserControl
    x:Class="Nidikwa.GUI.AltIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:local="clr-namespace:Nidikwa.GUI"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <ui:SymbolIcon>
        <ui:SymbolIcon.Style>
            <Style
                TargetType="ui:SymbolIcon">
                <Setter
                    Property="Symbol"
                    Value="{Binding RelativeSource={RelativeSource AncestorType=local:AltIcon}, Path=Symbol}" />
                <Style.Triggers>
                    <DataTrigger
                        Binding="{Binding RelativeSource={RelativeSource AncestorType=local:AltIcon}, Path=AltMode}"
                        Value="True">
                        <Setter
                            Property="Symbol"
                            Value="{Binding RelativeSource={RelativeSource AncestorType=local:AltIcon}, Path=AltSymbol}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ui:SymbolIcon.Style>
    </ui:SymbolIcon>
</UserControl>
